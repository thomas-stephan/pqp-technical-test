import { makeSx } from '../../../theme/utils'

export const sxs = makeSx({
  MovieCardList: {
    width: '100%',
  },
  MovieCardListContent: {
    overflowX: 'auto',
    paddingBottom: '1rem',
  },
})
